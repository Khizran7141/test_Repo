# Backend - Rate Limit API

## Overview
This is a simple Express.js backend that exposes an endpoint with custom rate limiting and blocking logic.

## How to Run

1. Navigate to the backend directory:
   ```bash
   cd backend
   npm install
   node index.js
   http://localhost:5000/api/ping